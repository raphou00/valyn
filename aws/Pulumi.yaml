name: valyn
description: aws infra for valyn app
runtime:
    name: nodejs
    options:
        packagemanager: npm
        nodeargs: "--loader ts-node/esm --no-warnings"
config:
    pulumi:tags:
        value:
            pulumi:template: aws-typescript
